#include "displayer.h"

void Displayer::Display() {
    for(int y = 0; y < Terrain.getHeight(); y++) {
        cout << "| ";
        for(int x = 0; x < Terrain.getWidth(); x++) {
            
        }
    }
}